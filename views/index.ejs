<!DOCTYPE html>
<html>
<head>
  <% include ../partials/head.ejs %>
  <title>Humans of Bloomington</title>
</head>
<body>
  <div class="wrapper">
  <nav>
    <ul class="nav" role="navigation">
      <% if (user) { %>
        <li><a href="/humansofbloomington/new">Create New Profile</a></li>
        <li>Signed in as <%= user %></li>
        <li>
        <form action="/humansofbloomington/session/logout?_method=DELETE" method="POST">
        <input type="submit" class="btn btn-default" value="Logout">
        </form>
        </li>
        <% } else { %>
          <li><a href="/humansofbloomington/session/login">Login</a></li>
          <li><a href="/humansofbloomington/session/register">Register</a></li>
          <% } %>
        </ul>
      </nav>
      <header>
        <h1>HUMANS OF BLOOMINGTON</h1>
      </header>
      <div class="container">
        <% for (let human of allHumans) { %>
          <div class="row">
          <div class="col">
          <div class="view"><a href="/humansofbloomington/<%= human.id %>"><img class="img-responsive" src="<%= human.image %>"></a><div class="mask"</div></div>
          <h4><%= human.name %> | <%= human.currentRole %></h4>
          </div>
          </div>
          <% } %>
        </div>
        <div class="push"></div>
      </div>
        <footer class="footer">
          <% include ../partials/footer.ejs %>
        </footer>
      </body>
      </html>
