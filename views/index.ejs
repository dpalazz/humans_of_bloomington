<!DOCTYPE html>
<html>
<head>
  <% include ../partials/head.ejs %>
  <title>Humans of Bloomington</title>
</head>
<body>
  <nav>
    <ul class="nav nav-pills" role="navigation">
      <% if (user) { %>
        <li role="presentation"><a href="/humansofbloomington/new">Create New Profile</a></li>
        <li role="presentation">Singed in as <%= user %></li>
        <li role="Presentation">
        <form action="/humansofbloomington/session/logout?_method=DELETE" method="POST">
        <input type="submit" class="btn btn-default" value="Logout">
        </form>
        </li>
        <% } else { %>
          <li role="presentation"><a href="/humansofbloomington/session/login">Login</a></li>
          <li role="presentation"><a href="/humansofbloomington/session/register">Register</a></li>
          <% } %>
        </ul>
      </nav>
      <header>
        <h1>HUMANS OF BLOOMINGTON</h1>
      </header>
      <div class="container">
        <% for (let human of allHumans) { %>
          <div class="row">
          <div class="col">
            <a href="/humansofbloomington/<%= human.id %>"><img class="img-responsive" src="<%= human.image %>"></a>
            <h4><%= human.name %> | <%= human.currentRole %></h4>
          </div>
          </div>
          <% } %>
        </div>
        <footer class="footer">
          <% include ../partials/footer.ejs %>
        </footer>
      </body>
      </html>
